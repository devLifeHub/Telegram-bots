import { FC } from "react"
import Logo from "@/components/atoms/Logo/Logo";

const Header: FC = () => {
    return (
        <header>
            <Logo />
        </header>
    )
}

export default Header